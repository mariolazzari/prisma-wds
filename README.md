# Prisma by WebDevSimplified

## Project configuration

```sh
pnpm init
pnpm add -D prisma typescript ts-node @types/node nodemon
tsc --init  
```

## Prisma configuration

```sh
pnpx prisma init --datasource-provider postgresql

```